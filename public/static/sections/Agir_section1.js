import React from 'react'
import { connect } from 'react-redux'

export const Agir_section1 = (props) => {
  return (
    <div>agir_section1</div>
  )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(Agir_section1)